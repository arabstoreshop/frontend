# API URL
NEXT_PUBLIC_API_URL=https://api.naseem.beauty

# Meta Pixel ID (public — no token)
NEXT_PUBLIC_META_PIXEL_ID=your_pixel_id

# TikTok Pixel ID (public — no token)
NEXT_PUBLIC_TIKTOK_PIXEL_ID=your_pixel_id

# Snap Pixel ID (public — no token)
NEXT_PUBLIC_SNAP_PIXEL_ID=your_pixel_id
